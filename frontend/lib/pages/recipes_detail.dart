import 'package:flutter/material.dart';

class RecipesDetailPage extends StatelessWidget {
  const RecipesDetailPage({Key? key, required recipes}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
